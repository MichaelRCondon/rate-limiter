{
  "backend_url": "http://localhost:3000/api",
  "default_limit_count": 100,
  "default_period": "1h",
  "mongo_url": "mongodb://username:password@localhost:27017/ratelimiter",
  "redis_config": {
    "redis_url": "localhost:6379", 
    "redis_db": 0
  },
  "server_config": {
    "port": 8080,
    "read_timeout": "30s",
    "write_timeout": "30s", 
    "idle_timeout": "120s"
  }
}